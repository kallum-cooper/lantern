{
  "name": "lantern",
  "version": "0.1.0",
  "private": true,
  "description": "A simple visual IPAM and homelab inventory tool.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/ipam.js && node --check src/store.js && node --check src/persistence.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
